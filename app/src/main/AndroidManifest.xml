<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:name="Setting.ApplicationClass"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_game"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_game"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ball_Shooter"
        tools:targetApi="31">

        <!-- MainActivity là activity khởi chạy đầu tiên -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Khai báo DifficultySelectionActivity -->
        <activity android:name=".DifficultySelectionActivity" />

        <!-- Khai báo GameActivity -->
        <activity android:name=".GameActivity" />

        <activity android:name=".AutoModeActivity" />

        <activity android:name=".StoreActivity" />



    </application>
</manifest>
